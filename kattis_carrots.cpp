#include <iostream>
#include <math.h>
// #include <string>

// using namespace std;

void solve(){
    int n, p;
    scanf("%d %d", &n, &p);
    while(n--){
        char s[1000];
        scanf("%s", s);
    }
    printf("%d", p);
}

int main(){
    solve();
    return 0;
}
